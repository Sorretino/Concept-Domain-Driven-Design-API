import AdminRepository from "~/Infra/repositories/AdminRepository";
import type { AdminProps } from "~/Shared/PropTypes/AdminProps";

const UpdateAdmin = {
  // async execute(id: string, props: AdminProps) {
  //   try {
  //     return await AdminRepository.update(id, props);
  //   } catch (error: any) {
  //     throw new Error(error.message);
  //   }
  // },
};

export default UpdateAdmin;
